server:
  addr: ":8080"
  nng_url: "tcp://host.docker.internal:5555"
  db_path: "/data/dashboard.db"

reflector:
  name: "URFD Dashboard"
  description: "Universal Reflector Dashboard"
  modules:
    A: "Module A"
    B: "Module B"
    C: "Module C"

logging:
  level: "info"
  console: true

audio:
  enable: true
  path: "/usr/local/bin/audio"

voice:
  enable: true
  reflector_addr: "tcp://host.docker.internal:5556"
  control_addr: "tcp://host.docker.internal:6556"
  transmit_password: "rogerdoger"
  max_clients: 100
  opus_bitrate: 12000
  max_tx_duration: 120
