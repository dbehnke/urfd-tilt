# Local configuration overrides
config/local/

# Tilt temporary files
.tilt/

# MacOS system files
.DS_Store

# Ralph config (may contain API key)
.opencode/ralph-config.json

# Runtime data (audio files, logs, database)
data/
